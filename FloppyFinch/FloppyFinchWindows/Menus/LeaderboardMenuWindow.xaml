<Window x:Class="FloppyFinchGameModes.Menus.LeaderboardMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Floppy Finch" Height="auto" Width="auto" MinHeight="600" MinWidth="400">
    <Grid>
        <Canvas Cursor="Help" Height="15" Width="15" Background="Transparent" VerticalAlignment="Top"
                HorizontalAlignment="Right" Margin="8" ToolTipService.InitialShowDelay="100">
            <Line X1="0" Y1="0" X2="15" Y2="0" Stroke="Black" />
            <Line X1="15" Y1="0" X2="15" Y2="15" Stroke="Black" />
            <Canvas.ToolTip>
                <ToolTip Content="To adjust resolution resize window." />
            </Canvas.ToolTip>
        </Canvas>
        <Canvas Cursor="Help" Height="15" Width="15" Background="Transparent" VerticalAlignment="Bottom"
                HorizontalAlignment="Left"
                Margin="8" ToolTipService.InitialShowDelay="100">
            <Line X1="0" Y1="15" X2="15" Y2="15" Stroke="Black" />
            <Line X1="0" Y1="0" X2="0" Y2="15" Stroke="Black" />
            <Canvas.ToolTip>
                <ToolTip Content="To adjust resolution resize window." />
            </Canvas.ToolTip>
        </Canvas>


        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Grid.Column="0" x:Name="ButtonShopBack" Click="ButtonBack_OnClick"
                    Content="Back" Width="75"
                    Height="25"
                    HorizontalAlignment="Left" Margin="35 0 " />

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- first place -->
                <Border Grid.Row="0"
                        Background="Gold"
                        Margin="20,10,20,20"
                        CornerRadius="10"
                        Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🏆"
                                   FontSize="32"
                                   VerticalAlignment="Center"
                                   Margin="0,0,15,0" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="First Place"
                                       FontSize="24"
                                       FontWeight="Bold" />
                            <TextBlock Text="{Binding FirstPlacePlayer}"
                                       FontSize="18" />
                        </StackPanel>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding FirstPlaceScore}"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1"
                              Margin="20,0,20,20"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding OtherPlaces}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F0F0F0"
                                        Margin="0,0,0,10"
                                        CornerRadius="5"
                                        Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Position}"
                                                   FontSize="18"
                                                   FontWeight="Bold"
                                                   Margin="0,0,15,0" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding PlayerName}"
                                                   FontSize="16" />
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Score}"
                                                   FontSize="18"
                                                   FontWeight="Bold" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>