<Window x:Class="FloppyFinchGameModes.Menus.SettingsMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Floppy Finch" Height="auto" Width="auto" MinHeight="600" MinWidth="400">
    <Grid>
        <Canvas Cursor="Help" Height="15" Width="15" Background="Transparent" VerticalAlignment="Top"
                HorizontalAlignment="Right" Margin="8" ToolTipService.InitialShowDelay="100">
            <Line X1="0" Y1="0" X2="15" Y2="0" Stroke="Black" />
            <Line X1="15" Y1="0" X2="15" Y2="15" Stroke="Black" />
            <Canvas.ToolTip>
                <ToolTip Content="To adjust resolution resize window." />
            </Canvas.ToolTip>
        </Canvas>
        <Canvas Cursor="Help" Height="15" Width="15" Background="Transparent" VerticalAlignment="Bottom"
                HorizontalAlignment="Left"
                Margin="8" ToolTipService.InitialShowDelay="100">
            <Line X1="0" Y1="15" X2="15" Y2="15" Stroke="Black" />
            <Line X1="0" Y1="0" X2="0" Y2="15" Stroke="Black" />
            <Canvas.ToolTip>
                <ToolTip Content="To adjust resolution resize window." />
            </Canvas.ToolTip>
        </Canvas>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Grid.Column="0" x:Name="ButtonShopBack" Click="ButtonSettingsBack_OnClick"
                    Content="Back" Width="75"
                    Height="25"
                    HorizontalAlignment="Left" Margin="35 0 " />
            <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="35 0">
                <Label Content="Gameplay settings" Margin="0 5" FontSize="16" />
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Grid.Row="0" Content="Chose resolution preset:" Margin="0 10 0 0"/>
                    <ComboBox Grid.Column="1" Grid.Row="0" Margin="0 10 0 0">
                        <ComboBoxItem Content="1920 x 1200" />
                        <ComboBoxItem Content="1920 x 1080" />
                        <ComboBoxItem Content="600 x 400" />
                    </ComboBox>

                    <Label Grid.Column="0" Grid.Row="1" Content="Chose appearance:" Margin="0 10 0 0"/>
                    <ComboBox Grid.Column="1" Grid.Row="1" Margin="0 10 0 0">
                        <ComboBoxItem Content="Light Mode" />
                        <ComboBoxItem Content="Dark mode" />
                        <ComboBoxItem Content="Automatic" />
                    </ComboBox>

                    <Label Grid.Column="0" Grid.Row="2" Content="Chose language:" Margin="0 10 0 0"/>
                    <ComboBox Grid.Column="1" Grid.Row="2" Margin="0 10 0 0">
                        <ComboBoxItem Content="English" />
                        <ComboBoxItem Content="Українська" />
                    </ComboBox>
                </Grid>
            </StackPanel>
            <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="35 0">
                <Label Content="Account settings" Margin="0 5" FontSize="16" />
                <Button Content="Sign out" Margin="0 5" Height="30" />
                <Button Content="Clear account data" Margin="0 5" Height="30" />
                <Button Content="Delete account" Margin="0 5" Height="30" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>